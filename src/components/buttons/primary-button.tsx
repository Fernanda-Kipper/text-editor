import styled from "styled-components";

export const PrimaryButton = styled.button`
    min-width: 152px;
    height: 50px;
    padding: 16px 8px;

    background: var(--primary);
    border-radius: 8px;
`